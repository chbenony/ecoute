# écoute

A Music League song recommendation assistant. React/Vite client, Express/TypeScript server, SQLite storage, Anthropic API for recommendations.

## Working via GitHub issues

When completing a task assigned through a GitHub issue or comment (e.g. tagged via `@claude`), always open the pull request yourself once the change is committed and pushed — run `gh pr create` rather than leaving a link for a human to click.
